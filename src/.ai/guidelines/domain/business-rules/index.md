# Indice de Regras de Negocio

Nenhuma regra registrada ainda.

O `project-planner` (e o `product-analyst` ao longo do projeto) devem criar arquivos por tema neste diretorio (`<tema>.md`) e listar cada um aqui, no formato:

- `<tema>.md`: descricao curta do que o arquivo cobre.

Outros agentes devem ler apenas este indice e, a partir dele, abrir somente o(s) arquivo(s) de tema relevante(s) para a task atual.
