# 10.4 — Criar `ci4-best-practices`

**Categoria:** Skills — Criações | **Prioridade:** 🟡 Alta | **Esforço:** M
**Fonte:** DEEPSEEK

## Objetivo

Criar skill de boas práticas para CodeIgniter 4, equivalente ao `laravel-best-practices`. O usuário usa mais CI4 que Laravel.

## Especificação

### Estrutura da skill

Seguir o mesmo padrão de `laravel-best-practices`: uma SKILL.md principal + sub-regras por tema.

```
src/.agents/skills/ci4-best-practices/
├── SKILL.md
└── rules/
    ├── controllers.md
    ├── models.md
    ├── entities.md
    ├── validation.md
    ├── routes.md
    ├── migrations.md
    ├── services.md
    ├── filters.md
    ├── security.md
    ├── testing.md
    ├── database.md
    └── views.md
```

### SKILL.md (principal)

```markdown
---
name: ci4-best-practices
description: >
  Boas praticas para CodeIgniter 4. Use ao escrever, revisar ou refatorar codigo CI4.
  Cobre controllers, models, entities, validation, routes, migrations, services,
  filters, security, testing, database e views.
---

# CodeIgniter 4 Best Practices

## Quando usar
- Criando ou modificando controllers, models, entities, migrations, seeders, filters, services
- Escrevendo queries, validacoes, rotas ou testes
- Revisando codigo CI4
- Duvidas sobre padroes e convencoes do framework

## Regras por Tema

| Tema | Arquivo | Topicos |
|------|---------|---------|
| Controllers | `rules/controllers.md` | Convencoes, injecao de dependencia, respostas, filters |
| Models | `rules/models.md` | Finders, query builder, entidades, timestamps |
| Entities | `rules/entities.md` | Getters/setters, casting, hydratation |
| Validation | `rules/validation.md` | Rules, custom rules, mensagens, grupos |
| Routes | `rules/routes.md` | Route groups, filters, placeholders, RESTful |
| Migrations | `rules/migrations.md` | Forge, indices, FKs, rollback |
| Services | `rules/services.md` | Service layer, injecao, shared instances |
| Filters | `rules/filters.md` | Auth, CSRF, rate limit, CORS |
| Security | `rules/security.md` | XSS, CSRF, SQL injection, validacao de input |
| Testing | `rules/testing.md` | HTTP tests, database tests, mocks |
| Database | `rules/database.md` | Queries, joins, indices, performance, migrations |
| Views | `rules/views.md` | View cells, layouts, partials, dados |
```

### Conteúdo das sub-regras

Cada arquivo `rules/<tema>.md` segue o formato:

```markdown
# <Tema>

## Boas Praticas

### 1. Topico
- Use X para Y
- Evite Z (prefira W)

### 2. Topico
...

## Exemplos

```php
// Correto:
$model->where('status', 'active')->findAll();

// Evitar:
$db->query("SELECT * FROM table WHERE status = 'active'");
```
```

### Referência no stack guideline

Atualizar `src/.ai/guidelines/stacks/codeigniter4.md`:

```markdown
## Skills
- `ci4-best-practices`: use como referencia de padroes e boas praticas CodeIgniter 4.
```

## Arquivos Afetados

- `src/.agents/skills/ci4-best-practices/SKILL.md`: NOVO
- `src/.agents/skills/ci4-best-practices/rules/*.md`: NOVO (12 arquivos)
- `src/.ai/guidelines/stacks/codeigniter4.md`: adicionar referência à skill

## Critérios de Aceite

- [ ] `ci4-best-practices/SKILL.md` existe com tabela de referência para sub-regras
- [ ] 12 sub-regras em `rules/` — cada uma com boas práticas e exemplos
- [ ] `codeigniter4.md` referencia `ci4-best-practices` nas skills
- [ ] Estrutura segue o padrão de `laravel-best-practices`
- [ ] Conteúdo cobre padrões reais do CI4 (não genérico)
- [ ] Exemplos de código em PHP com sintaxe do CI4
