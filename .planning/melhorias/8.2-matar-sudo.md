# 8.2 — Matar [SUDO]

**Categoria:** Desacoplamento de Agentes | **Prioridade:** 🔴 Crítica | **Esforço:** S
**Fonte:** DEEPSEEK

## Objetivo

Remover o prefixo `[SUDO]` do `model-selection.md`. Sem restrição por agente (item 8.1), bypass é desnecessário. Também elimina uma backdoor de segurança — se um atacante injetar `[SUDO]` em dados, ignora todas as restrições de role.

## Situação Atual

`model-selection.md:14`:
```markdown
- Quando o humano usar o prefixo `[SUDO]`, Codex/GPT-5.5, Gemini e Copilot podem
  ser recomendados ou assumir cargos normalmente atribuidos ao Claude, conforme
  o override critico em `AGENTS.md`.
```

## Especificação

### Mudança 1: model-selection.md

Remover a linha 14 completamente.

### Mudança 2: AGENTS.md

Verificar se há referência a `[SUDO]` no AGENTS.md. Não há (confirmado na leitura do arquivo). Nenhuma ação necessária.

### Mudança 3: Roles

Verificar se alguma role referencia `[SUDO]`. Nenhuma referencia encontrada. Nenhuma ação necessária.

## Arquivos Afetados

- `src/.ai/guidelines/core/model-selection.md`: remover linha do `[SUDO]`

## Critérios de Aceite

- [ ] `model-selection.md` não contém a palavra "SUDO"
- [ ] `model-selection.md` não referencia override de roteamento por prefixo
- [ ] Nenhum outro arquivo do projeto referencia `[SUDO]`
- [ ] `grep -r "SUDO" src/` retorna vazio
